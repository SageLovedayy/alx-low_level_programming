#include "main.h"

/**
 * _memcpy - Copies n bytes from memory area src to memory area dest.
 * @dest: The destination memory area.
 * @src: The source memory area.
 * @n: The number of bytes to copy.
 * Return: A pointer to the destination memory area.
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	/* Your implementation here */
	return (dest);
}

/**
 * _strchr - Locates the first occurrence of the character c in the string s.
 * @s: The string to search.
 * @c: The character to locate.
 * Return: A pointer to the located character or NULL
 * if the character is not found.
 */
char *_strchr(char *s, char c)
{
	/* Your implementation here */
	return (NULL);
}

/**
 * _strspn - Calculates the length of the initial segment of s which
 * consists entirely of characters in accept.
 *
 * @s: The string to calculate the length of the segment.
 * @accept: The string of characters to match.
 * Return: The length of the segment.
 */
unsigned int _strspn(char *s, char *accept)
{
	/* Your implementation here */
	return (0);
}

/**
 * _strpbrk - Searches in the string s for any of the
 * characters in the string accept.
 *
 * @s: The string to search.
 * @accept: The string of characters to search for.
 * Return: A pointer to the first occurrence in s
 */
char *_strpbrk(char *s, char *accept)
{
	/* Your implementation here */
	return (NULL);
}

/**
 * _strstr - Locates the first occurrence of the entire string needle
 * in the string haystack.
 *
 * @haystack: add descr
 * @needle: add descr
 * Return: a pointer to the beginning of the located substring or
 * NULL if the substring is not found.
 */
char *_strstr(char *haystack, char *needle)
{
	/* Your implementation here */
	return (NULL);
}
